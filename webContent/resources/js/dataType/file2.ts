//TypeScript
function showCustomDialog(messageCode: number): boolean {
    return confirm("Message: " + messageCode);
}

//showDialog("Phuc Hieu")
//--------------------------
let result: boolean
//result = showCustomDialog(500);
//alert(result)